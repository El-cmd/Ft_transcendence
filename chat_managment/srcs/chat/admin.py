from django.contrib import admin
from .models import *

# Register your models here.

admin.register( )

# @admin.register( )
# class ChatRoomAdmin(admin.ModelAdmin):
    # list_display = ("name", "user_1", "user_2", )
    # list_filter = ("name")
    # search_fields = ("name", )
